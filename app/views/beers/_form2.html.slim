= form_for @rating, :html => { :id => "rating" } do |f|
  - if @rating.errors.any?
    #error_explanation
      h2 = "#{pluralize(@beer.errors.count, "error")} prohibited this beer from being saved:"
      ul
        - @rating.errors.full_messages.each do |message|
          li = message
  .star
  .hidden#oldrating data-rating="#{@rating.stars}"
  /= f.number_field :stars, :placeholder=> "1-5, 5 being highest"
  = f.hidden_field :user_id, :value => current_user.id
  = f.hidden_field :beer_id, :value => @beer.id
  br
  = f.text_area :review, :placeholder=>"What'd you think of the beer? (optional)"
  .actions = f.submit 'Save', :class=> "btn"
