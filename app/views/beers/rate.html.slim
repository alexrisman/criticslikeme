h1 ="Rate #{@beer.name}"
p.text-center
  =image_tag @beer.photo.url, :class=>"beer"
  ==render "form2"
  br
  -if (@nextbeer)
    =link_to "Next Beer", beers_rate_path(@nextbeer), :class=>"btn"
  -if (current_user.ratings.count > 2)
    =link_to "View Recommendations", beers_recommend_path, :class=>"btn btn-warning"
-if (notice)
  p.alert = notice